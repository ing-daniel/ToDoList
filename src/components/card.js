import styled from 'styled-components';

export const Card = styled.div`
    width: 100%;
    background: white;
    min-height: 60px;
    border-radius: .4em;
    box-shadow: 0 10px 20px rgba(0,0,0,.3);
    padding: 1rem;
    position:relative;
`;